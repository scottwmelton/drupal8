<?php

/**
 * @file
 * Contains testscott.module.
 */

use Drupal\Core\Routing\RouteMatchInterface;

/**
 * Implements hook_help().
 */
function testscott_help($route_name, RouteMatchInterface $route_match) {
  switch ($route_name) {
    // Main module help for the testscott module.
    case 'help.page.testscott':
      $output = '';
      $output .= '<h3>' . t('About') . '</h3>';
      $output .= '<p>' . t('My Awesome Module') . '</p>';
      return $output;

    default:
  }
}

/**
 * Implements hook_theme().
 */
function testscott_theme() {
  return [
    'testscott' => [
      'render element' => 'children',
    ],
  ];
}
